<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:background="@color/colorBackground">

    <ImageView
        android:id="@+id/icon_category"
        android:layout_width="72dp"
        android:layout_height="72dp"
        android:elevation="@dimen/normal_elevation"
        android:layout_marginStart="5dp"
        android:layout_marginEnd="@dimen/normal_margin"
        android:layout_marginTop="4dp"
        android:background="@drawable/selector_category_item_recycler"
        android:padding="@dimen/large_margin"
        android:src="@drawable/ic_filter"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:tint="@color/selector_item_category_ic" />

    <TextView
        android:id="@+id/category_label"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/min_margin"
        android:textColor="@color/selector_item_category_text"
        android:textSize="@dimen/small_text_size"
        android:textStyle="bold"
        app:layout_constraintEnd_toEndOf="@id/icon_category"
        app:layout_constraintStart_toStartOf="@id/icon_category"
        app:layout_constraintTop_toBottomOf="@id/icon_category"
        tools:text="Filter" />
</androidx.constraintlayout.widget.ConstraintLayout>